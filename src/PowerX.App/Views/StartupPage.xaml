<Page NavigationCacheMode="Enabled"
    x:Class="PowerX.App.Views.StartupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="2">
            <TextBlock Text="Startup" Style="{StaticResource PageTitleStyle}" />
            <TextBlock x:Name="Summary" Style="{StaticResource MutedStyle}" Text="Scanning…" />
        </StackPanel>

        <Border x:Name="BootCard" Grid.Row="1" Style="{StaticResource CardStyle}" Visibility="Collapsed">
            <Grid ColumnSpacing="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Spacing="4" VerticalAlignment="Center">
                    <TextBlock x:Name="BootHeadline" Style="{StaticResource BodyStrongTextBlockStyle}" />
                    <TextBlock x:Name="BootDetail" Style="{StaticResource MutedStyle}" FontSize="12" TextWrapping="Wrap" />
                </StackPanel>
                <StackPanel x:Name="BootTrendPanel" Grid.Column="1" Spacing="4" VerticalAlignment="Center" Visibility="Collapsed">
                    <StackPanel x:Name="BootTrend" Orientation="Horizontal" Spacing="3" Height="46" HorizontalAlignment="Right" />
                    <TextBlock x:Name="BootTrendLabel" Style="{StaticResource MutedStyle}" FontSize="11" HorizontalAlignment="Right" />
                </StackPanel>
            </Grid>
        </Border>

        <InfoBar Grid.Row="2" IsOpen="True" Severity="Informational" IsClosable="False"
                 Message="Disabling keeps the entry and you can turn it back on any time. This is the same mechanism Task Manager uses. Remove deletes the registry value, and is offered for RunOnce entries and for entries whose program no longer exists." />

        <Grid Grid.Row="3" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="Filter" Grid.Column="0" PlaceholderText="Search startup entries…" TextChanged="Filter_Changed" />
            <ToggleButton x:Name="BrokenOnly" Grid.Column="1" Content="Missing programs only" Click="BrokenOnly_Click" />
        </Grid>

        <ScrollViewer Grid.Row="4" Style="{StaticResource PageScrollStyle}">
            <StackPanel x:Name="List" Spacing="6" Padding="0,0,0,16" />
        </ScrollViewer>
    </Grid>
</Page>
