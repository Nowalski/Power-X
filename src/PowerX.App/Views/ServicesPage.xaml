<Page NavigationCacheMode="Enabled"
    x:Class="PowerX.App.Views.ServicesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PowerX.App.Views">
    <Grid RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="2">
            <TextBlock Text="Services" Style="{StaticResource PageTitleStyle}" />
            <TextBlock x:Name="Summary" Style="{StaticResource MutedStyle}" Text="Loading…" />
        </StackPanel>

        <Grid Grid.Row="1" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="Filter" Grid.Column="0" PlaceholderText="Search services…" TextChanged="Filter_Changed" />
            <ComboBox x:Name="ShowBox" Grid.Column="1" MinWidth="150" SelectedIndex="0" SelectionChanged="Show_Changed">
                <ComboBoxItem Content="All" />
                <ComboBoxItem Content="Running" />
                <ComboBoxItem Content="Stopped" />
                <ComboBoxItem Content="Automatic" />
            </ComboBox>
        </Grid>

        <ListView Grid.Row="2" x:Name="List" SelectionMode="None" ShowsScrollingPlaceholders="True">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0,3" />
                    <Setter Property="MinHeight" Value="0" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:ServiceVm">
                    <Border Style="{StaticResource CardStyle}" Padding="14,10">
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Spacing="2">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="{x:Bind DisplayName}" Style="{StaticResource BodyStrongTextBlockStyle}" VerticalAlignment="Center" />
                                    <Border Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="4" Padding="6,1" VerticalAlignment="Center">
                                        <TextBlock Text="{x:Bind StatusText, Mode=OneWay}" FontSize="11" />
                                    </Border>
                                    <TextBlock Text="critical" FontSize="11" Foreground="{ThemeResource SystemFillColorCautionBrush}"
                                               VerticalAlignment="Center" Visibility="{x:Bind CriticalVisibility}" />
                                </StackPanel>
                                <TextBlock Text="{x:Bind Description, Mode=OneWay}" FontSize="12" MaxLines="2" TextTrimming="CharacterEllipsis"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <TextBlock FontSize="11" Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                           Text="{x:Bind NameAndStartMode, Mode=OneWay}" />
                            </StackPanel>

                            <Button Grid.Column="1" VerticalAlignment="Center" IsEnabled="{x:Bind RowEnabled, Mode=OneWay}"
                                    Content="{x:Bind StartStopLabel, Mode=OneWay}" Tag="{x:Bind Name}" Click="StartStop_Click" />
                            <Button Grid.Column="2" VerticalAlignment="Center" Content="…" Padding="10,6" Tag="{x:Bind Name}">
                                <Button.Flyout>
                                    <MenuFlyout>
                                        <MenuFlyoutItem Text="Restart" Tag="{x:Bind Name}" Click="Restart_Click" />
                                        <MenuFlyoutSeparator />
                                        <MenuFlyoutItem Text="Start type: Automatic" Tag="{x:Bind Name}" Click="ModeAuto_Click" />
                                        <MenuFlyoutItem Text="Start type: Automatic (delayed)" Tag="{x:Bind Name}" Click="ModeDelayed_Click" />
                                        <MenuFlyoutItem Text="Start type: Manual" Tag="{x:Bind Name}" Click="ModeManual_Click" />
                                        <MenuFlyoutItem Text="Start type: Disabled" Tag="{x:Bind Name}" Click="ModeDisabled_Click" />
                                        <MenuFlyoutSeparator />
                                        <MenuFlyoutItem Text="Open in services.msc" Click="OpenMsc_Click" />
                                    </MenuFlyout>
                                </Button.Flyout>
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
